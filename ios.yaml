push:
  categories:
    - name: Alarm home Ståle
      identifier: 'alarm_home_stale'
      actions:
        - identifier: 'DISARM_ALARM'
          title: 'Skru av alarm'
          activationMode: 'background'
          authenticationRequired: yes
          destructive: yes
          behavior: 'default'

    - name: Alarm away Ståle
      identifier: 'alarm_away_stale'
      actions:
        - identifier: 'ARM_ALARM'
          title: 'Skru på alarm'
          activationMode: 'background'
          authenticationRequired: yes
          destructive: no
          behavior: 'default'
